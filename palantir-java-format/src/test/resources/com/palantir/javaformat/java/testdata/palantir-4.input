class Palantir4 {
    void foo() {
        if (useAddedColumnsSetDesription) {
            resultSet = spark.sparkSetCreator().resolve(initialSetResolver, new LoremIpsumLongClassNameWithDescription(
                    initialSet, list, Optional.empty()));
        }
    }
}


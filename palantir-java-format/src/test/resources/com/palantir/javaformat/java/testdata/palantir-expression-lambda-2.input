class PalantirExpressionLambda2 {
    void foo() {
        this(txnManager, foundryResources, commitLogHandler, timestamp ->
                new UUID(timestamp, UUID.randomUUID().getLeastSignificantBits()));
    }
}
